import { Components } from '@mui/material'

const MuiAutocomplete: Components['MuiAutocomplete'] = {
  styleOverrides: { root: { '& .MuiInputBase-root': { padding: 0 } } },
}

export default MuiAutocomplete
